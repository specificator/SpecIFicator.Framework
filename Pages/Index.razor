@using SpecIFicator.Framework.RazorComponents
@page "/"
@layout EmptyLayout

<PageTitle>SpecIFicator</PageTitle>

@if (_specIfDataProviderFactory.MetadataReader == null)
{
    <CascadingValue Value="_dataContext">
        <SpecIfDataConnector></SpecIfDataConnector>
    </CascadingValue>
}
else
{
    _navigationManager.NavigateTo("/projects");    
}

